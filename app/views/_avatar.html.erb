<% if user.avatar.attached? %>
  <%= image_tag(user.avatar.variant(resize_to_limit:[150,150]), id: avatar)%>
  <% elsif !user.avatar_url.nil?%>
    <%= image_tag(user.avatar_url, id: avatar)%>
  <%else%>
  <%= image_tag(url_for("/assets/user.png"), id: avatar)%>
  <%end%>
  