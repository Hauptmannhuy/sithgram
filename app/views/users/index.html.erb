<div class="container-users">
  <%= render '/search-form', path: users_path %>
  <div class="users-list">
    
    <% @users.each_with_index do |user,i|%>
        <div class="user-req-tab">
          <%= render '/avatar_link',user: user%>
          <div><%= user.username%></div>
          <%= render '/request-buttons', user: user, i:i, requested_users: @requested_users, pending_users: @pending_users unless user.id == current_user.id || @friend_ids.include?(user.id) %>
        </div>
    <%end%>
    
  </div>
</div>
